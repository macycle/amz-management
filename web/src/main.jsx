import React,{useEffect,useState}from'react';
import{createRoot}from'react-dom/client';
import{Boxes,Package,RefreshCw,LogOut,Plus,X,ChevronRight,AlertTriangle}from'lucide-react';
import'./styles.css';
const api=async(path,options={})=>{const token=localStorage.token;const r=await fetch('/api'+path,{...options,headers:{'Content-Type':'application/json',...(token?{Authorization:`Bearer ${token}`}:{})}});if(r.status===401){localStorage.clear();location.reload()}const data=r.status===204?null:await r.json();if(!r.ok)throw Error(data.error||'请求失败');return data};
const money=v=>v==null?'—':`¥${Number(v).toFixed(2)}`;
function Login({onLogin}){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function submit(e){e.preventDefault();try{const r=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.token=r.token;localStorage.user=JSON.stringify(r.user);onLogin(r.user)}catch(e){setError(e.message)}}return <main className="login"><section className="hero"><div className="brand"><Boxes/> AMZ 管理</div><div className="hero-copy"><p>精细运营　优化成本</p><p>提升效率　把控利润</p></div></section><section className="login-panel"><form onSubmit={submit}><h1>账号登录</h1><p className="muted">欢迎回来，请登录管理您的采购与库存</p><label>邮箱<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>密码<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="error">{error}</p>}<button>登录</button></form></section></main>}
function Password({done}){const[currentPassword,setCurrent]=useState(''),[newPassword,setNext]=useState(''),[error,setError]=useState('');return <div className="modal-back"><form className="modal" onSubmit={async e=>{e.preventDefault();try{await api('/auth/password',{method:'POST',body:JSON.stringify({currentPassword,newPassword})});localStorage.user=JSON.stringify({...JSON.parse(localStorage.user),mustChangePassword:false});done()}catch(e){setError(e.message)}}}><h2>请修改初始密码</h2><label>当前密码<input type="password" value={currentPassword} onChange={e=>setCurrent(e.target.value)} required/></label><label>新密码（至少 8 位）<input type="password" minLength="8" value={newPassword} onChange={e=>setNext(e.target.value)} required/></label>{error&&<p className="error">{error}</p>}<button>保存新密码</button></form></div>}
function ProductModal({value,close,saved}){const[d,setD]=useState({marketplace:value.marketplace||'US',asin:value.asin||'',name:value.name||'',imageUrl:value.image_url||'',status:value.status||'active',notes:value.notes||'',amazonInventory:value.amazon_inventory||0}),[error,setError]=useState('');const set=(k,v)=>setD({...d,[k]:v});return <div className="modal-back"><form className="modal wide" onSubmit={async e=>{e.preventDefault();try{await api(value.id?'/products/'+value.id:'/products',{method:value.id?'PUT':'POST',body:JSON.stringify({...d,marketplace:d.marketplace.toUpperCase(),asin:d.asin.toUpperCase(),amazonInventory:Number(d.amazonInventory)})});saved()}catch(e){setError(e.message)}}}><h2>{value.id?'编辑产品':'新增产品'}</h2><label>产品名称<input value={d.name} onChange={e=>set('name',e.target.value)} required/></label><label>亚马逊站点（如 US、CA、UK）<input value={d.marketplace} onChange={e=>set('marketplace',e.target.value.toUpperCase())} minLength="2" maxLength="12" required/></label><label>ASIN（10 位）<input value={d.asin} onChange={e=>set('asin',e.target.value.toUpperCase())} maxLength="10" required/></label><label>亚马逊库存<input type="number" min="0" value={d.amazonInventory} onChange={e=>set('amazonInventory',e.target.value)} required/></label><label>产品图片 URL<input type="url" value={d.imageUrl} onChange={e=>set('imageUrl',e.target.value)}/></label><label>状态<select value={d.status} onChange={e=>set('status',e.target.value)}><option value="active">启用</option><option value="inactive">停用</option></select></label><label>备注<textarea value={d.notes} onChange={e=>set('notes',e.target.value)}/></label>{error&&<p className="error">{error}</p>}<div className="actions"><button type="button" className="ghost" onClick={close}>取消</button><button>保存</button></div></form></div>}
function SimpleModal({type,close,save}){const[d,setD]=useState(type==='sku'?{sku:'',variantName:'',inventory:0}:{supplierSku:'',purchaseUrl:'',specification:'',enabled:true}),[error,setError]=useState('');const set=(k,v)=>setD({...d,[k]:v});return <div className="modal-back inner"><form className="modal" onSubmit={async e=>{e.preventDefault();try{await save(type==='sku'?{...d,inventory:Number(d.inventory)}:d)}catch(e){setError(e.message)}}}><h2>{type==='sku'?'新增 Amazon SKU':'关联 1688 SKU'}</h2>{type==='sku'?<><label>SKU<input value={d.sku} onChange={e=>set('sku',e.target.value)} required/></label><label>变体名称<input value={d.variantName} onChange={e=>set('variantName',e.target.value)}/></label><label>库存<input type="number" min="0" value={d.inventory} onChange={e=>set('inventory',e.target.value)}/></label></>:<><label>供应商 SKU<input value={d.supplierSku} onChange={e=>set('supplierSku',e.target.value)} required/></label><label>1688 采购链接<input type="url" value={d.purchaseUrl} onChange={e=>set('purchaseUrl',e.target.value)} required/></label><label>规格<input value={d.specification} onChange={e=>set('specification',e.target.value)}/></label></>}{error&&<p className="error">{error}</p>}<div className="actions"><button type="button" className="ghost" onClick={close}>取消</button><button>保存</button></div></form></div>}
function Detail({product,close,changed,remove,setNotice}){const[adding,setAdding]=useState(null),[editing,setEditing]=useState(false);const addSku=async d=>{await api('/products/'+product.id+'/skus',{method:'POST',body:JSON.stringify(d)});setAdding(null);changed()};const addSupplier=async(id,d)=>{await api('/amazon-skus/'+id+'/suppliers',{method:'POST',body:JSON.stringify(d)});setAdding(null);changed()};return <div className="drawer-back"><aside className="drawer"><button className="icon close" onClick={close}><X/></button><div className="detail-title"><span className="eyebrow">{product.marketplace} · ASIN · {product.asin}</span><h2>{product.name}</h2><p>亚马逊库存 <b>{product.amazon_inventory}</b>　状态：{product.status==='active'?'启用':'停用'}</p><div className="detail-actions"><button className="ghost" onClick={()=>setEditing(true)}>编辑产品</button><button className="danger" onClick={remove}>停用产品</button></div></div><div className="mapping-head"><h3>SKU 配对</h3><button onClick={()=>setAdding({type:'sku'})}><Plus size={15}/>新增 Amazon SKU</button></div>{product.skus.map(a=><div className="amazon-card" key={a.id}><div className="sku-line"><b>{a.sku}</b><span>{a.variant_name||'默认变体'} · 库存 {a.inventory}</span><button className="link" onClick={()=>setAdding({type:'supplier',id:a.id})}>+ 关联 1688 SKU</button></div>{a.suppliers.map(s=><div className="supplier" key={s.id}><div><b>{s.supplier_sku}</b><small>{s.specification||'未填写规格'} · {money(s.current_price)} · 供应库存 {s.current_stock??'—'}</small><a href={s.purchase_url} target="_blank" rel="noreferrer">打开采购链接</a></div><button className="secondary mini" onClick={async()=>{try{const r=await api('/suppliers/'+s.id+'/sync',{method:'POST'});setNotice(`同步成功：${money(r.price)}，库存 ${r.stock}`);changed()}catch(e){setNotice('同步失败：'+e.message)}}}><RefreshCw size={14}/>同步</button></div>)}</div>)}{adding&&<SimpleModal type={adding.type} close={()=>setAdding(null)} save={d=>adding.type==='sku'?addSku(d):addSupplier(adding.id,d)}/>} {editing&&<ProductModal value={product} close={()=>setEditing(false)} saved={()=>{setEditing(false);changed()}}/>}</aside></div>}
function Products({setNotice}){const[rows,setRows]=useState([]),[search,setSearch]=useState(''),[selected,setSelected]=useState(null),[form,setForm]=useState(null);const load=()=>api('/products?search='+encodeURIComponent(search)).then(r=>setRows(r.items));useEffect(()=>{load()},[search]);async function open(id){setSelected(await api('/products/'+id))}async function remove(id){if(!confirm('停用并隐藏这个产品？其同步历史仍会保留。'))return;await api('/products/'+id,{method:'DELETE'});setNotice('产品已停用');setSelected(null);load()}return <><div className="toolbar"><div className="search">⌕<input placeholder="搜索站点、ASIN 或产品名称" value={search} onChange={e=>setSearch(e.target.value)}/></div><button onClick={()=>setForm({})}><Plus size={16}/>新增产品</button></div><section className="panel table-wrap"><table><thead><tr><th>产品</th><th>站点</th><th>ASIN</th><th>亚马逊库存</th><th>SKU 数</th><th>状态</th><th/></tr></thead><tbody>{rows.map(p=><tr key={p.id} onClick={()=>open(p.id)}><td><b>{p.name}</b></td><td><span className="marketplace">{p.marketplace}</span></td><td>{p.asin}</td><td>{p.amazon_inventory}</td><td>{p.sku_count}</td><td><em className={p.status}>{p.status==='active'?'启用':'停用'}</em></td><td><ChevronRight size={17}/></td></tr>)}{!rows.length&&<tr><td colSpan="7" className="empty">暂无产品，请新增第一条产品记录。</td></tr>}</tbody></table></section>{form&&<ProductModal value={form} close={()=>setForm(null)} saved={()=>{setForm(null);load();setNotice('产品已保存')}}/>}{selected&&<Detail product={selected} close={()=>setSelected(null)} changed={()=>{open(selected.id);load()}} remove={()=>remove(selected.id)} setNotice={setNotice}/>}</>}
function Dashboard({setNotice}){const[data,setData]=useState(null),[syncing,setSyncing]=useState(false);const load=()=>api('/dashboard').then(setData);useEffect(()=>{load()},[]);if(!data)return <p>加载中…</p>;const cards=[['产品总数',data.products,Package],['亚马逊缺货',data.out_of_stock,AlertTriangle],['供应风险',data.supplier_risk,AlertTriangle],['同步失败',data.failures,RefreshCw]];return <><div className="toolbar"><div><h3>库存与采购概览</h3><p className="muted">1688 自动同步：每周一 09:00（北京时间）</p></div><button className="secondary" disabled={syncing} onClick={async()=>{setSyncing(true);try{const r=await api('/sync/all',{method:'POST'});setNotice(`同步完成：${r.success} 成功，${r.failed} 失败`);load()}finally{setSyncing(false)}}}><RefreshCw size={16}/>{syncing?'同步中…':'立即同步全部'}</button></div><div className="cards">{cards.map(([l,n,I])=><article className="card" key={l}><span><I size={20}/></span><p>{l}</p><strong>{n}</strong></article>)}</div></>}
function App(){const[user,setUser]=useState(()=>JSON.parse(localStorage.user||'null')),[tab,setTab]=useState('dashboard'),[notice,setNotice]=useState('');if(!user)return <Login onLogin={setUser}/>;return <div className="app"><aside><div className="logo"><Boxes/> AMZ 管理</div><nav><button className={tab==='dashboard'?'active':''} onClick={()=>setTab('dashboard')}>▦　运营看板</button><button className={tab==='products'?'active':''} onClick={()=>setTab('products')}>▣　产品与库存</button></nav><div className="side-bottom"><span>{user.email}</span><button onClick={()=>{localStorage.clear();setUser(null)}}><LogOut size={16}/>退出登录</button></div></aside><main className="content"><header><div><h2>{tab==='dashboard'?'运营看板':'产品与库存'}</h2><p>亚马逊库存与 1688 采购数据统一管理</p></div>{notice&&<div className="toast">{notice}<X size={15} onClick={()=>setNotice('')}/></div>}</header>{tab==='dashboard'?<Dashboard setNotice={setNotice}/>:<Products setNotice={setNotice}/>}</main>{user.mustChangePassword&&<Password done={()=>setUser(JSON.parse(localStorage.user))}/>}</div>}
createRoot(document.getElementById('root')).render(<App/>);
